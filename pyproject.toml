[project]
name = "apple-tools"
version = "0.1.0"
description = "Collection of tools for working with Applesoft BASIC for the Apple ]["
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = [
    "graphics",
    "preprocess",
]
